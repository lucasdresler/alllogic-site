---
import { navigationContent } from "../../content/navigation";
import "./navigation.css";

const items = navigationContent.main;
---

<nav aria-label="Principal">
  <ul class="navigation__list">
    {items.map((item) => (
      <li>
        <a href={item.href}>{item.label}</a>
      </li>
    ))}
  </ul>
</nav>
